{
  "containerOverrides": [{
    "name": "saleor-manage",
    "command": ["python manage.py shell -c \"from saleor.account.models import User; u=User(email='admin@learnwithvinay.in', is_staff=True, is_superuser=True, is_active=True); u.set_password('Admin@123'); u.save(); print(f'Superuser created: {u.email}')\""]
  }]
}
