{
  "containerOverrides": [{
    "name": "saleor-manage",
    "command": ["python manage.py shell -c \"from saleor.account.models import User; u=User.objects.get(email='admin@learnwithvinay.in'); u.set_password('Admin@123'); u.save(); print('Password set')\""]
  }]
}
