{
  "containerOverrides": [{
    "name": "saleor-manage",
    "command": ["python manage.py shell -c \"from django.contrib.sites.models import Site; s=Site.objects.get_current(); s.domain='dev.learnwithvinay.in'; s.name='Saleor Dev'; s.save(); print(f'Site updated: {s.domain}')\""]
  }]
}
