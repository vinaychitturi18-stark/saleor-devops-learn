# Copy this file to secrets.yaml and fill in real values
# secrets.yaml is in .gitignore and should NEVER be committed
secrets:
  postgresPassword: "changeme"
  secretKey: "changeme-very-long-random-secret-key-here"
  emailUrl: "smtp://localhost"
