server {
    listen       80;
    server_name  localhost;
    root   /app;

    location /dashboard/ {
        try_files $uri $uri/ /dashboard/index.html;
    }

    location / {
        rewrite ^/$ /dashboard/ redirect;
        try_files $uri $uri/ /dashboard/index.html;
    }
}
